<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.scene.shape.*?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      stylesheets="@/Styles/login.css"
      styleClass="login-container"
      fx:controller="com.rep.controller.views.LoginController"
      alignment="CENTER"
      spacing="20">

    <!-- Encabezado con icono de emoji -->
    <VBox styleClass="header" alignment="CENTER" spacing="10">
        <HBox alignment="CENTER" spacing="10">
            <StackPane>
                <Circle styleClass="icon-circle" radius="30"/>
                <Text text="👦" styleClass="kids-decoration"/>
            </StackPane>
            <StackPane>
                <Circle styleClass="icon-circle" radius="30"/>
                <Text text="👧" styleClass="kids-decoration"/>
            </StackPane>
        </HBox>
        <Text styleClass="icon-text" text="Aprende Jugando"/>
    </VBox>

    <!-- Formulario -->
    <VBox styleClass="form-panel" alignment="CENTER" spacing="15">
        <HBox alignment="CENTER" spacing="5">
            <Label text="🔒" styleClass="kids-decoration"/>
            <TextField fx:id="txtIdentificacion"
                       styleClass="input-field"
                       promptText="Tu número de identificación"/>
        </HBox>

        <HBox alignment="CENTER" spacing="5">
            <Label text="🔑" styleClass="kids-decoration"/>
            <PasswordField fx:id="txtPassword"
                           styleClass="input-field"
                           promptText="Tu contraseña secreta"/>
        </HBox>

        <Button fx:id="btnLogin"
                onAction="#handleLogin"
                styleClass="main-button"
                text="¡Entrar al Juego!"
                maxWidth="Infinity"/>

        <ProgressIndicator fx:id="progressIndicator"
                           visible="false"
                           styleClass="progress-indicator"/>
    </VBox>

    <!-- Pie de página -->
    <HBox styleClass="footer" alignment="CENTER" spacing="5">
        <Label styleClass="footer-label" text="¿No tienes cuenta?"/>
        <Button fx:id="btnRegistro"
                mnemonicParsing="false"
                styleClass="link-button"
                onAction="#registro"
                text="¡Regístrate aquí!"/>
    </HBox>

    <Label fx:id="lblMensaje" styleClass="message-label"/>
</VBox>